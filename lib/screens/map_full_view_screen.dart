import 'package:flutter/material.dart';

class MapFullViewScreen extends StatelessWidget {
  const MapFullViewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Kigali Map View')),
      body: const Center(child: Text('Google Maps Integration Coming Soon.')),
    );
  }
}